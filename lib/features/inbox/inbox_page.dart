import 'package:flutter/material.dart';

class InboxPage extends StatelessWidget {
  const InboxPage({super.key, required Map<String, String> product});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}

